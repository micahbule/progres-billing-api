import { EntityTransformerInterceptor } from './entity-transformer.interceptor';

describe('EntityTransformerInterceptor', () => {
  it('should be defined', () => {
    expect(new EntityTransformerInterceptor()).toBeDefined();
  });
});
